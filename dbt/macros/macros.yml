macros:

  - name: get_variation_percent
    description: Calculate the variation percentage between and earliest and latest.
    arguments:
      - name: latest
        type: number
      - name: earliest
        type: number

  - name: get_last_saturday_date
    description: Get the last Saturday date from the execution date
